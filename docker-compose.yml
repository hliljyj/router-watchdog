services:
  router-watchdog:
    build: .
    container_name: router-watchdog
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - watchdog-logs:/var/log

volumes:
  watchdog-logs:
